@model IEnumerable<ClubReg.Models.Socios>

@{
    ViewBag.Title = "Index";
}

<h2>Socios</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nombres)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Apellidos)
        </th>
        
        <th>
            @Html.DisplayNameFor(model => model.Membresia)
        </th>
        
        <th>
            @Html.DisplayNameFor(model => model.EstMen)
        </th>
        
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nombres)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Apellidos)
        </td>
       
        <td>
            @Html.DisplayFor(modelItem => item.Membresia)
        </td>
        
        <td>
            @Html.DisplayFor(modelItem => item.EstMen)
        </td>
        
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SocioID }) |
            @Html.ActionLink("Details", "Details", new { id=item.SocioID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SocioID })
        </td>
    </tr>
}

</table>
